ifeq ($(CONFIG_OTA),y)
    CSRCS += ota.c
    VPATH += :ota

#ifeq ($(CONFIG_HTTP_CLIENT),y)
    CSRCS += http_ota.c
#endif

ifeq ($(CONFIG_OTA_LOCAL),y)
	CSRCS += local_ota.c
endif


ifeq ($(CONFIG_OTA_SERVICE),y)
	CSRCS += mqtt_ota.c
endif
endif
